import styled from 'styled-components';
import { Container } from '@chakra-ui/react';

export const ContainerStylized = styled(Container)`
  margin-top: 10%;
  padding: 2px 2px 2px 2px;
  border-style: hidden;
`